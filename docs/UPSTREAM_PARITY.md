# Upstream parity manifest

> GENERATED by `tools/upstream/gen_upstream_parity.py` from
> `tools/upstream/parity_manifest.json` — edit the manifest, not this file.
> CI regenerates and diffs it (`--check`), so hand edits fail the build.

This is the current, versioned upstream-coverage view required by
[#189](https://github.com/thetechgeekko/Spektrafilm-android/issues/189).
Dated sync/porting plans (e.g. `UPSTREAM_SYNC_2026-06-24.md`) are historical
input, not coverage claims.

## Pins

- **Upstream:** [https://github.com/andreavolpato/spektrafilm](https://github.com/andreavolpato/spektrafilm) (GPL-3.0-only)
- **Oracle (numeric contract):** `c1d0e44` — docs: update high-res banner (2026-05-23).
  Every committed golden reproduces bit-exactly here and only here; the
  first diverging child is `a9bccd6`.
- **Reviewed upstream main:** `3bb2c2d` (tree `0aaea99`) — fix: vlog midgray exposure (2026-06-06), reviewed 2026-09-01.
- **Bundled assets:** 41 files under `engine/spektra-core/src/main/assets/spektra`, tree digest `d44c4a92d122494f…`
- **Track-only branches:** `couplers-tuning-iterations@a273e49`, `dev@6cd00c8`, `experimental@28bf883`.

## Status vocabulary

| Status | Meaning |
|---|---|
| matched | 1:1 with the oracle revision; parity-gated |
| adapted | Same capability, deliberately different mechanism or defaults on Android |
| extension | Android-only; no upstream counterpart |
| inapplicable | Not applicable here — desktop-only, or ruled out by the pinned baseline; rationale given |
| missing | Applicable and absent; owned by the linked atomic ticket |

**Totals:** 13 matched, 7 adapted, 5 extension, 8 inapplicable, 1 missing (34 items).

## Runtime stages and taps

| Upstream item | Status | Android / evidence |
|---|---|---|
| Filming stage (RGB → spectral via Hanatos2025 LUT → camera raw → film density CMY, DIR couplers, exposure) | matched | runtime/stages/filming.cpp; gates `filming`, `simulate_e2e` (both film-density memos), `highlight_boost_e2e`, `provia_couplers_e2e` |
| Printing stage (film CMY → enlarger dichroic Y/M/C → print paper density) | matched | runtime/stages/printing.cpp; gates `simulate_e2e` (print-density memo), `enlarger_lut_e2e`, `print_evcomp_e2e`, `print_spatial_e2e` |
| Scanning stage (density → spectral radiance → CIE XYZ → output RGB, viewing illuminants incl. K75P, NaN profile-null propagation) | matched | runtime/stages/scanning.cpp; gates `simulate_e2e`, `scanner_lut_e2e`, `output_spaces`, `scanner_bwcorr_e2e`; -fno-finite-math-only preserved for NaN semantics |
| Crop/resize and auto-exposure metering stages | matched | runtime/stages/crop_resize.cpp, autoexposure.cpp; gates `crop_resize`, `downscale`, `autoexposure`, `small_preview_aa` |
| Diffusion (halation + in-emulsion scatter) and lens blur, incl. FFT convolution path | matched | model/diffusion.cpp; gates `diffusion`, `diffusion_e2e`, `fft_convolve`, `lensblur`, `spatial`, `spatial_decouple_e2e` |
| Grain (Poisson-binomial particle model, sublayers, micro-structure) | matched | model/grain.cpp; statistical gates `test_grain`, `test_grain_sublayer`, `test_binomial_shortcircuit` (std::mt19937 can never byte-match numpy, so the gate is mean/std vs committed oracle references) |
| Runtime tap inject/collect (`topology.py`) and `lut_mode` deterministic bake regime | adapted | Equivalent mechanism, different plumbing: `spk_simulate_tap` for named taps, `spk_bake_cube_lut` for the LUT-bake regime; gates `bake_lut`, `params_passthrough`, per-tap goldens |

## Parameters and defaults

| Upstream item | Status | Android / evidence |
|---|---|---|
| Full parameter surface and defaults of oracle c1d0e44 (camera, film, enlarger, print, scanner, grain, couplers, diffusion, glare, tone curves) | matched | runtime/params_manifest.h; gate `params_passthrough` plus `simulate_e2e` per-param key completeness (every key perturbs the memo) |
| Spektral opt-in controls (spectral gaussian blur, Hanatos surface, camera UV/IR band-pass, preflash, print EV compensation, scanner B/W correction) | matched | Gates `spectral_blur_e2e`, `hanatos_surface_e2e`, `camera_uvir_e2e`, `preflash_e2e`, `print_evcomp_e2e`, `scanner_bwcorr_e2e` |
| np.interp semantics on the non-monotonic DIR-coupler axis | matched | Gate `np_interp` (verbatim upstream semantics) |
| GUI-selectable RGB→raw method Mallett2019; film-side glare quartet; enlarger lens blur | missing | Marshalled but inert on Android: `SPK_RGB2RAW_MALLETT2019` has no dispatch (memo-key change only), film `glare_*` has no film-side consumer, `enlarger_lens_blur` has no call site — ticket https://github.com/thetechgeekko/Spektrafilm-android/issues/143 |

## Profiles and assets

| Upstream item | Status | Android / evidence |
|---|---|---|
| All 28 film/paper profiles, spectral LUTs and neutral print filters at oracle c1d0e44 | matched | Bundled under engine/spektra-core/src/main/assets/spektra (tree digest pinned in upstream_pin.json); loaders io/npy_lut.cpp + profiles/; docs-consistency counts 27 presets |
| Profile JSON refit of 28/29 stocks + neutral_print_filters.json (commit ad5c8d2, deltas to 0.43 ≈ 570× parity tolerance) | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §5 — *Changes the default rendered look of every existing photo; adopting it is the deliberate Strategy-B rebaseline (docs/UPSTREAM_SYNC_2026-06-24.md §5), not a piecemeal port. The default render stays byte-pinned to oracle c1d0e44 per the owner-approved #126 contract* |

## GUI-visible behavior

| Upstream item | Status | Android / evidence |
|---|---|---|
| napari desktop GUI (sliders/param tiers per param_manifest.py) | adapted | Jetpack Compose Lightroom-style editor: category tabs, sliders with value semantics, presets/recipes, profile-curve browser, crop/masks; accessibility-gated (#181). Param coverage proven by `params_passthrough`, not by mirroring the desktop layout |
| Desktop preview-max slider clamp (removed upstream in 83278a5) | adapted | Android owns its independent two-regime preview: 640 px interactive preview vs full-res scan export; upstream's desktop clamp does not apply |

## LUT creation, import and export formats

| Upstream item | Status | Android / evidence |
|---|---|---|
| Deterministic LUT bake of the film look (lut_mode) | matched | `spk_bake_cube_lut`; gate `bake_lut` |
| LUT export for external tools | adapted | Android exports .cube and CLF (app ClfWriter.kt, lutFileName); scope limited to the two formats with Android consumers |
| spektrafilm_lut_creator (~15k LOC: 3dl, HaldCLUT, lumix builders, ocio_emit, QA suite) | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §6 — *Desktop LUT/OCIO authoring tooling with zero engine math and no Android consumer (docs/UPSTREAM_SYNC_2026-06-24.md §6)* |

## Android-only extensions

| Upstream item | Status | Android / evidence |
|---|---|---|
| — | extension | RAW/DNG import via patched LibRaw 0.22.2 through an ACES intermediate → linear ProPhoto (lib:libraw, oracle-locked CAT02 white balance #192), hostile-corpus sanitizer/fuzz gates (#165) |
| — | extension | Fail-closed OutputDescriptor export matrix (#174): JPEG, PNG, PNG16, TIFF16, TIFF32F, scene-linear TIFF32F; Ultra HDR explicit and release-blocked (#140); 16-bit native writers lib:tiffwriter/lib:pngwriter |
| — | extension | Masks/local adjustments, presets/recipes with versioned transactional storage (#170), process-death recovery (#139), foreground export service, render-local stage timing `spk.stage_timings.v1` (#163) |
| — | extension | Capability-gated Fast GPU Vulkan route (pointwise resident chain) separate from the Strict Exact CPU route; tolerance-qualified, never presented as bit-identical (#126 contract, docs/research/gpu-bit-exact.md) |
| — | extension | Spectral 3D-LUT memo + byte-budgeted tc_lut_cache LRU (#142); gates `lut_cache_e2e`, `lut_accel`, `tc-lut` stats surface |

## Reviewed upstream delta (c1d0e44 → 3bb2c2d)

| Upstream item | Status | Android / evidence |
|---|---|---|
| Output gamut compression, algorithms aces_rgc / oklch / oklrab (default-ON upstream at 3bb2c2d) | adapted | Ported as default-OFF opt-ins so the c1d0e44 default render stays byte-identical; gates `gamut_out_aces`, `gamut_out_oklch`, `gamut_out_oklrab` with newer-oracle goldens |
| Output gamut compression, remaining perceptual algorithms jzazbz / cam16ucs (cam16ucs is the upstream default) | matched | Ported default-OFF as opt-ins with 3bb2c2d-oracle goldens (#201); gates `gamut_out_jzazbz`, `gamut_out_cam16ucs`; upstream's fixed one-sided _compress_lightness applied |
| Input gamut compression: xy locus bake portion | adapted | Ported default-OFF; gate `gamut_in_xy` |
| Input-side CAT02→CAT16 swap + lrgb/xy hard-clip removal; color_reference and spectral_upsampling/calibration_targets CAT16 rework | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §4 — *Default-path changes whose 3bb2c2d goldens cannot be isolated from the ad5c8d2 profile refit and the a9bccd6 raw-scaling drift without new profile-injection tooling; tractable only under the Strategy-B rebaseline (docs/UPSTREAM_SYNC_2026-06-24.md §4/§5)* |
| Print density-curve morph (morph_curves.py / develop_print_morph) | adapted | Ported default-OFF (upstream replaces the tabulated print interp on the default path); gate `print_curves_morph` (opt-in s023 morph) |
| Grain default-value refit (particle_scale/density_min/uniformity tuples) + agx_* → particle_* rename | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §4 (grain refit) — *Math already 1:1 (rename only); adopting the refit VALUES changes the default grain look of every render — a default-look change reserved for the deliberate rebaseline. docs/UPSTREAM_SYNC_2026-06-24.md §4 calls skipping it defensible (purely aesthetic)* |
| DIR-coupler None-guard and diffusion sigma_um<=0 guard | matched | Python None-division guards; impossible/already short-circuited in C++ (couplers.cpp:211, filming.cpp lens-blur gate) |
| color_reference gamma_factor → 1.0 in the negative-print B/W reference measurement | inapplicable | spektra.cpp measure_print_references call; gate `scanner_bwcorr_e2e` — *Deliberately NOT applied: the shipped bwcorr path byte-matches oracle c1d0e44 (which used gamma_factor=pg) and its golden pins that; applying the fix would break `scanner_bwcorr_e2e` without a newer-oracle golden. Carried on the rebaseline list; default print_density_curve_gamma=1.0 makes it a no-op in practice* |
| vlog midgray fix (3bb2c2d), GC black-level fix (6ec371b), preview-max removal (83278a5) | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §6 — *vlog hunk lives only in desktop lut_mode/deactivate_spatial branches absent from the port; the GC black-level fix targets code that does not exist at c1d0e44 (it is folded into #201's acceptance); preview-max is a desktop GUI clamp* |
| params_schema/params_builder/pipeline/profiles-io refactors, param_manifest.py GUI metadata, OCIO configs | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md §6 — *Python-internal restructuring and desktop color-management emit with no engine math and no Android consumer; the profile-metadata GPLv3→CC BY-SA relicense is recorded in upstream_pin.json* |

## Experimental upstream branches

| Upstream item | Status | Android / evidence |
|---|---|---|
| N-channel/B&W rework, kodak_trix/doublex/2302, parametric density_curves_model, convert stage, grain overhaul, Langmuir couplers, taking filters, WB normalization, new upsampling methods (dev/experimental/couplers-tuning-iterations) | inapplicable | docs/UPSTREAM_SYNC_2026-06-24.md Part B; upstream_pin.json track_only_branches — *Unmerged, actively iterating author R&D whose own defaults are in flux; several studies ship as opaque baked LUTs with no generator code. Track-only per docs/UPSTREAM_SYNC_2026-06-24.md Part B; branch SHAs pinned in upstream_pin.json. Revisit only after upstream merges to main and defaults stabilize* |

*Film modeling powered by spektrafilm (GPLv3).*
